kind = "web"
previewPath = "/"
title = "Budgetarian"
version = "1.0.0"
id = "artifacts/budgetarian"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20419

[services.development]
run = "pnpm --filter @workspace/budgetarian run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/budgetarian", "run", "build" ]
publicDir = "artifacts/budgetarian/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20419"
BASE_PATH = "/"
